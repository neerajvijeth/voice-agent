# voice_agent/requirements.txt
# Pin approximate versions that are known to work together.
# Install with:  pip install -r requirements.txt

# ── Audio capture & playback ──────────────────────────────────────────────────
sounddevice>=0.4.6
numpy>=1.24

# ── Voice Activity Detection ──────────────────────────────────────────────────
webrtcvad>=2.0.10   # lightweight, fast, C extension
setuptools<70       # required by webrtcvad which uses deprecated pkg_resources
# Alternative: silero-vad (better accuracy, needs torch)
# pip install silero-vad torch torchaudio

# ── Speech-to-Text ────────────────────────────────────────────────────────────
faster-whisper>=1.0.0
# CTranslate2 is a dependency of faster-whisper – installed automatically

# ── TTS (install the backend you want) ───────────────────────────────────────
# Option 1: edge-tts (free cloud, great quality)
edge-tts>=6.1.9

# Option 2: pyttsx3 (offline, robotic)
pyttsx3>=2.90

# Option 3: piper – install binary separately (see README / setup.sh)
# piper is a standalone binary, not a pip package

# ── Audio decoding for edge-tts MP3 output ────────────────────────────────────
pydub>=0.25.1
# pydub requires ffmpeg system binary:
#   macOS:  brew install ffmpeg
#   Linux:  sudo apt install ffmpeg
#   Windows: https://ffmpeg.org/download.html

# ── LLM (Google Gemini API) ───────────────────────────────────────────────────
google-genai>=1.0.0
python-dotenv>=1.0.0

# ── RAG (Retrieval Augmented Generation) — pgvector ──────────────────────────
python-docx>=1.1.0
python-pptx>=1.0.0
psycopg2-binary>=2.9.0    # PostgreSQL driver
pgvector>=0.3.0            # pgvector Python bindings
sentence-transformers>=3.0.0  # embedding model

# ── GPU acceleration ─────────────────────────────────────────────────────────
# PyTorch with CUDA — install separately for your CUDA version:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
# Do NOT install torch from this file — the default pip version is CPU-only.
